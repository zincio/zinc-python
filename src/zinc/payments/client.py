# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.checkout_session_response import CheckoutSessionResponse
from ..types.pending_payment_response import PendingPaymentResponse
from .raw_client import AsyncRawPaymentsClient, RawPaymentsClient


class PaymentsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawPaymentsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawPaymentsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawPaymentsClient
        """
        return self._raw_client

    def get_pending_payment(
        self, payment_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PendingPaymentResponse:
        """
        Where a card payment stands; poll it after a 402 ``payment_required``.

        ``requires_action`` until the buyer pays on ``pay_url``; then ``authorized``
        and, as soon as the order is created from the parked draft, ``placed`` with
        ``order_id``. The poll itself does the creating when it gets there before
        the webhook, so a buyer who pays and comes straight back sees the order.

        Parameters
        ----------
        payment_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PendingPaymentResponse
            Successful Response

        Examples
        --------
        from zinc import ZincClient

        client = ZincClient(
            api_key="YOUR_API_KEY",
        )
        client.payments.get_pending_payment(
            payment_id="payment_id",
        )
        """
        _response = self._raw_client.get_pending_payment(payment_id, request_options=request_options)
        return _response.data

    def create_checkout_session(
        self, payment_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> CheckoutSessionResponse:
        """
        The Stripe-hosted payment page for a pending payment.

        Called by pay.zinc.com when the buyer clicks through, not by the 402 itself,
        so a link nobody opens never creates a Stripe session. Idempotent per
        payment: a session already open is returned again.

        Parameters
        ----------
        payment_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CheckoutSessionResponse
            Successful Response

        Examples
        --------
        from zinc import ZincClient

        client = ZincClient(
            api_key="YOUR_API_KEY",
        )
        client.payments.create_checkout_session(
            payment_id="payment_id",
        )
        """
        _response = self._raw_client.create_checkout_session(payment_id, request_options=request_options)
        return _response.data


class AsyncPaymentsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawPaymentsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawPaymentsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawPaymentsClient
        """
        return self._raw_client

    async def get_pending_payment(
        self, payment_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PendingPaymentResponse:
        """
        Where a card payment stands; poll it after a 402 ``payment_required``.

        ``requires_action`` until the buyer pays on ``pay_url``; then ``authorized``
        and, as soon as the order is created from the parked draft, ``placed`` with
        ``order_id``. The poll itself does the creating when it gets there before
        the webhook, so a buyer who pays and comes straight back sees the order.

        Parameters
        ----------
        payment_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PendingPaymentResponse
            Successful Response

        Examples
        --------
        import asyncio

        from zinc import AsyncZincClient

        client = AsyncZincClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.payments.get_pending_payment(
                payment_id="payment_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_pending_payment(payment_id, request_options=request_options)
        return _response.data

    async def create_checkout_session(
        self, payment_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> CheckoutSessionResponse:
        """
        The Stripe-hosted payment page for a pending payment.

        Called by pay.zinc.com when the buyer clicks through, not by the 402 itself,
        so a link nobody opens never creates a Stripe session. Idempotent per
        payment: a session already open is returned again.

        Parameters
        ----------
        payment_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CheckoutSessionResponse
            Successful Response

        Examples
        --------
        import asyncio

        from zinc import AsyncZincClient

        client = AsyncZincClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.payments.create_checkout_session(
                payment_id="payment_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_checkout_session(payment_id, request_options=request_options)
        return _response.data
