# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .order_payment_info_mode import OrderPaymentInfoMode


class OrderPaymentInfo(UniversalBaseModel):
    """
    How a card-funded order is paid, and where its Stripe hold stands.
    Present only for ``payment.mode == "card"`` orders; ``null`` for wallet and
    Connect orders (Connect has its own ``connect`` block). All amounts in cents.

    ``state`` moves ``authorized`` → ``captured`` when the retailer order is
    placed (``captured_cents`` = actual total + fee), or → ``released`` when the
    order fails or is cancelled first. The card is only ever charged
    ``captured_cents``; the difference to ``authorized_cents`` is released.
    """

    mode: typing.Optional[OrderPaymentInfoMode] = None
    state: str = pydantic.Field()
    """
    authorized | captured | released | refunded | …
    """

    authorized_cents: int
    fee_cents: int
    stripe_fee_cents: typing.Optional[int] = pydantic.Field(default=None)
    """
    Card-processing fee grossed into the amount above, the same way a wallet top-up and a Connect charge gross it in.
    """

    captured_cents: typing.Optional[int] = None
    payment_method: typing.Optional[str] = None
    payment_intent_id: typing.Optional[str] = None
    simulated: typing.Optional[bool] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
