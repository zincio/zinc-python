# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .order_milestone import OrderMilestone
from .order_status import OrderStatus


class OrderTimelineResponse(UniversalBaseModel):
    """
    Customer-facing lifecycle timeline for an order.

    Derived on read by merging placement logs (OrderLog) with carrier tracking
    state (TrackingNumber / TrackingCheckpoint). No dedicated storage — works
    retroactively on every historical order.
    """

    order_id: str
    current_status: OrderStatus
    milestones: typing.List[OrderMilestone]

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
