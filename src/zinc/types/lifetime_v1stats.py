# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .lifetime_v1stats_source import LifetimeV1StatsSource


class LifetimeV1Stats(UniversalBaseModel):
    """
    All-time totals from the legacy v1 API (api.zinc.io).

    ``source: "computed"`` — read from the snapshot the weekly worker job
    stores (see ``lib.tasks.v1_lifetime``); ``source: "seeded"`` — the
    in-code cold-start fallback constant.
    """

    orders_successful: int
    gmv_cents: int
    as_of: dt.date
    source: typing.Optional[LifetimeV1StatsSource] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
