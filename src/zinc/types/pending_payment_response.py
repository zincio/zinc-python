# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .payment_quote import PaymentQuote
from .pending_payment_client import PendingPaymentClient
from .pending_payment_product import PendingPaymentProduct
from .pending_payment_response_status import PendingPaymentResponseStatus


class PendingPaymentResponse(UniversalBaseModel):
    """
    A card payment the buyer still has to complete, or just did.
    """

    id: str
    status: PendingPaymentResponseStatus
    order_id: typing.Optional[str] = pydantic.Field(default=None)
    """
    Set once the order exists (status 'placed').
    """

    pay_url: typing.Optional[str] = pydantic.Field(default=None)
    """
    Where the buyer pays: the Stripe Checkout page for this payment (or Zinc's hosted page, which opens it, if Stripe could not be reached when the order was posted). Null once the payment no longer needs the buyer.
    """

    expires_at: dt.datetime
    quote: PaymentQuote
    products: typing.List[PendingPaymentProduct]
    client: typing.Optional[PendingPaymentClient] = pydantic.Field(default=None)
    """
    Set when the caller said which assistant the buyer is using (X-Zinc-Client on POST /orders).
    """

    ship_to_summary: str = pydantic.Field()
    """
    Recipient, city, state and postcode — enough for the payment page to show what is being bought without exposing the full address.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
