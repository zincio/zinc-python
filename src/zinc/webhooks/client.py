# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.webhook_endpoint_response import WebhookEndpointResponse
from .raw_client import AsyncRawWebhooksClient, RawWebhooksClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class WebhooksClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawWebhooksClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawWebhooksClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawWebhooksClient
        """
        return self._raw_client

    def get_webhook_endpoint(
        self, *, authorization: typing.Optional[str] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> WebhookEndpointResponse:
        """
        The URL Zinc delivers this account's webhooks to, and the HMAC secret
        that signs them. Both are null until ``PUT /webhooks/endpoint`` is called.

        Parameters
        ----------
        authorization : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WebhookEndpointResponse
            Successful Response

        Examples
        --------
        from zinc import ZincClient

        client = ZincClient(
            api_key="YOUR_API_KEY",
        )
        client.webhooks.get_webhook_endpoint()
        """
        _response = self._raw_client.get_webhook_endpoint(authorization=authorization, request_options=request_options)
        return _response.data

    def set_webhook_endpoint(
        self,
        *,
        url: str,
        authorization: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> WebhookEndpointResponse:
        """
        Register (or replace) the webhook URL for this account.

        Every order and return event Zinc emits for the account is POSTed to this
        URL. A signing secret is generated on first registration and returned so
        the caller can verify the ``X-Webhook-Signature`` header; replacing the URL
        keeps the existing secret, so a URL move never invalidates verification.

        Parameters
        ----------
        url : str
            Absolute http(s) URL that receives order and return events.

        authorization : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WebhookEndpointResponse
            Successful Response

        Examples
        --------
        from zinc import ZincClient

        client = ZincClient(
            api_key="YOUR_API_KEY",
        )
        client.webhooks.set_webhook_endpoint(
            url="https://example.com/zinc/webhook",
        )
        """
        _response = self._raw_client.set_webhook_endpoint(
            url=url, authorization=authorization, request_options=request_options
        )
        return _response.data

    def clear_webhook_endpoint(
        self, *, authorization: typing.Optional[str] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Stop webhook delivery for this account by clearing the URL.

        The signing secret is kept, so re-registering a URL later resumes
        deliveries signed with the same secret the caller already verifies against.

        Parameters
        ----------
        authorization : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from zinc import ZincClient

        client = ZincClient(
            api_key="YOUR_API_KEY",
        )
        client.webhooks.clear_webhook_endpoint()
        """
        _response = self._raw_client.clear_webhook_endpoint(
            authorization=authorization, request_options=request_options
        )
        return _response.data


class AsyncWebhooksClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawWebhooksClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawWebhooksClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawWebhooksClient
        """
        return self._raw_client

    async def get_webhook_endpoint(
        self, *, authorization: typing.Optional[str] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> WebhookEndpointResponse:
        """
        The URL Zinc delivers this account's webhooks to, and the HMAC secret
        that signs them. Both are null until ``PUT /webhooks/endpoint`` is called.

        Parameters
        ----------
        authorization : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WebhookEndpointResponse
            Successful Response

        Examples
        --------
        import asyncio

        from zinc import AsyncZincClient

        client = AsyncZincClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.webhooks.get_webhook_endpoint()


        asyncio.run(main())
        """
        _response = await self._raw_client.get_webhook_endpoint(
            authorization=authorization, request_options=request_options
        )
        return _response.data

    async def set_webhook_endpoint(
        self,
        *,
        url: str,
        authorization: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> WebhookEndpointResponse:
        """
        Register (or replace) the webhook URL for this account.

        Every order and return event Zinc emits for the account is POSTed to this
        URL. A signing secret is generated on first registration and returned so
        the caller can verify the ``X-Webhook-Signature`` header; replacing the URL
        keeps the existing secret, so a URL move never invalidates verification.

        Parameters
        ----------
        url : str
            Absolute http(s) URL that receives order and return events.

        authorization : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WebhookEndpointResponse
            Successful Response

        Examples
        --------
        import asyncio

        from zinc import AsyncZincClient

        client = AsyncZincClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.webhooks.set_webhook_endpoint(
                url="https://example.com/zinc/webhook",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.set_webhook_endpoint(
            url=url, authorization=authorization, request_options=request_options
        )
        return _response.data

    async def clear_webhook_endpoint(
        self, *, authorization: typing.Optional[str] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Stop webhook delivery for this account by clearing the URL.

        The signing secret is kept, so re-registering a URL later resumes
        deliveries signed with the same secret the caller already verifies against.

        Parameters
        ----------
        authorization : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from zinc import AsyncZincClient

        client = AsyncZincClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.webhooks.clear_webhook_endpoint()


        asyncio.run(main())
        """
        _response = await self._raw_client.clear_webhook_endpoint(
            authorization=authorization, request_options=request_options
        )
        return _response.data
