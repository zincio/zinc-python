# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.wallet_response import WalletResponse
from .raw_client import AsyncRawWalletClient, RawWalletClient


class WalletClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawWalletClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawWalletClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawWalletClient
        """
        return self._raw_client

    def get_wallet(
        self, *, authorization: typing.Optional[str] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> WalletResponse:
        """
        Get your wallet balance.

        All amounts are integer cents. `balance` is the ledger balance;
        `spendable_balance` is what `POST /orders` actually checks against (the two
        differ only for Zinc Connect accounts with in-flight holds). An order needs
        `max_price + order_fee_cents` spendable, so compare against that before
        placing one instead of discovering a shortfall as a 402. Bulk-deal customers
        (`billed_by_invoice: true`) are invoiced monthly and skip the balance check.

        Under a `zn_test_` key (or `X-Test-Mode`) this reads the sandbox wallet,
        which sandbox orders never draw down. Funds are added from the dashboard.

        Parameters
        ----------
        authorization : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WalletResponse
            Successful Response

        Examples
        --------
        from zinc import ZincClient

        client = ZincClient(
            api_key="YOUR_API_KEY",
        )
        client.wallet.get_wallet()
        """
        _response = self._raw_client.get_wallet(authorization=authorization, request_options=request_options)
        return _response.data


class AsyncWalletClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawWalletClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawWalletClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawWalletClient
        """
        return self._raw_client

    async def get_wallet(
        self, *, authorization: typing.Optional[str] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> WalletResponse:
        """
        Get your wallet balance.

        All amounts are integer cents. `balance` is the ledger balance;
        `spendable_balance` is what `POST /orders` actually checks against (the two
        differ only for Zinc Connect accounts with in-flight holds). An order needs
        `max_price + order_fee_cents` spendable, so compare against that before
        placing one instead of discovering a shortfall as a 402. Bulk-deal customers
        (`billed_by_invoice: true`) are invoiced monthly and skip the balance check.

        Under a `zn_test_` key (or `X-Test-Mode`) this reads the sandbox wallet,
        which sandbox orders never draw down. Funds are added from the dashboard.

        Parameters
        ----------
        authorization : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WalletResponse
            Successful Response

        Examples
        --------
        import asyncio

        from zinc import AsyncZincClient

        client = AsyncZincClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.wallet.get_wallet()


        asyncio.run(main())
        """
        _response = await self._raw_client.get_wallet(authorization=authorization, request_options=request_options)
        return _response.data
