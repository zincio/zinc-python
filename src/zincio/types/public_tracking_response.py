# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .order_status import OrderStatus
from .public_tracking_destination import PublicTrackingDestination
from .public_tracking_item import PublicTrackingItem
from .public_tracking_shipment import PublicTrackingShipment


class PublicTrackingResponse(UniversalBaseModel):
    """
    Public, unauthenticated tracking view for an order.

    Keyed by the order's random UUID (used as an unguessable share token). Shows
    the status, destination, per-package carrier checkpoints, and the ordered
    items — everything except the recipient's name and phone.
    """

    order_id: str
    current_status: OrderStatus
    estimated_delivery_date: typing.Optional[str] = pydantic.Field(default=None)
    """
    Best-known order-level delivery estimate (destination-local calendar date); per-package estimates are on each shipment.
    """

    destination: PublicTrackingDestination
    shipments: typing.List[PublicTrackingShipment]
    items: typing.List[PublicTrackingItem]

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
