# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class CustomerNotificationStatus(UniversalBaseModel):
    """
    End-customer email-notification status for an order.

    Present only when the order opted into the notifications add-on. ``delivered``
    is the deliverability signal: True once a notification reached the shopper
    (an unsubscribe click also counts — it proves receipt), False if one was
    attempted but bounced/failed/suppressed, and null while nothing has been
    sent yet (e.g. not shipped) or a send is still in flight.
    """

    email: str
    delivered: typing.Optional[bool] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
